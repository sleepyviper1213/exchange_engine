#pragma once
// Forward declarations for the trade event submodule (scaffold).

namespace exchange::engine::event::trade {

// TODO: declare the trade event types (TradeExecuted, ...) as they are added.

} // namespace exchange::engine::event::trade
