#pragma once
// Forward declarations for the lifecycle event submodule (scaffold).

namespace exchange::engine::event::lifecycle {

// TODO: declare the lifecycle event types (Startup, Shutdown, ...) as they
// are added.

} // namespace exchange::engine::event::lifecycle
