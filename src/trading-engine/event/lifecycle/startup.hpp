#pragma once
// Engine start-up event (scaffold).
//
// Emitted when the matching engine transitions to running, so persistence and
// downstream consumers can mark a session boundary. Not implemented yet; this
// header only fixes the module's shape and namespace.

namespace exchange::engine::event::lifecycle {

// TODO: define the start-up event type.
// struct Startup { ... };

} // namespace exchange::engine::event::lifecycle
