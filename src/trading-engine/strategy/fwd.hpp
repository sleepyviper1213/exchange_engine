#pragma once
// Forward declarations for the strategy submodule (scaffold).

namespace exchange::engine::strategy {

// TODO: declare the strategy types (Iceberg, Stop, ...) as they are added.

} // namespace exchange::engine::strategy
