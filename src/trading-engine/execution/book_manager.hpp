#pragma once
// Book manager (scaffold).
//
// Owns the per-symbol order books (BTCUSDT/ETHUSDT/etc.) and routes each
// command to the matching engine responsible for that symbol. Not implemented
// yet; this header only fixes the module's shape and namespace.

namespace exchange::engine::execution {

// TODO: define the per-symbol book manager.
// class BookManager { ... };

} // namespace exchange::engine::execution
